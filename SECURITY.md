# Security

This is a public template repository.
- Do **not** commit secrets (tokens, credentials, keys).
- Use `.env.example` for local variables and Databricks Secret Scopes for workspace secrets.
